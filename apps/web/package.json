{
  "name": "@skillcdn/web",
  "version": "0.0.0",
  "private": true,
  "description": "The web UI: landing page and explorer. Static files, prerendered per language.",
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --ssr src/entry-server.tsx --outDir dist/render && node scripts/prerender.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "vite",
    "generate:fonts": "node scripts/generate-pretendard-css.mjs",
    "dev:api": "vite --mode api",
    "dev:live": "vite --mode live",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@skillcdn/core": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.0",
    "pretendard": "^1.3.9",
    "typescript": "catalog:",
    "vite": "^8.0.0",
    "vitest": "catalog:"
  }
}
