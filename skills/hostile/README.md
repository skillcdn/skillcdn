# Hostile fixtures

Deliberately broken skills. A reader must skip each broken one, report why, and still serve `skills/valid-neighbor`. `skills/Loud_Name` is served with warnings. Oversized inputs are generated inside the tests instead of being committed.
